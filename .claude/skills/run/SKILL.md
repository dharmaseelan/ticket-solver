# Run the dev server

Start the ticket-solver app locally.

1. Check that `.env.local` exists and has `ANTHROPIC_API_KEY` set. If missing, warn the user.
2. Run `npm run dev` from the project root (`/Users/dharma/Downloads/ticket-solver`).
3. Confirm the server is running at http://localhost:3000.
